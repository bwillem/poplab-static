import React from 'react'
import {
  FixedWidthRow,
  BlueText,
} from '../uiLibrary'

export const Second = props => {
  return (
      <h2>
        <BlueText>poplab</BlueText> is a blah blah blah
      </h2>
  );
}